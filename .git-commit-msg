docs(10-keeler-top): T1's review record, and its box

Verdict pass. Five scenarios, five tests named after them; the two that
had to observe a negative say in the record how they do it.

Two defects. CI's clippy step lost `--workspace` and was linting the
root package alone — fixed in 18e4bdb, out of T1's file list and taken
anyway because no spec-10 task owns that file. The other is left open
for the user: the spec's prose says the second-init signal catches a
resume that wrote past the old offset, and it cannot, because `tee`
truncates and the new run's init lands below the offset. The scenario
the Acceptance Tests section states is met — the record proposes the two
ways out and makes no behavior change.

The box is ticked because the whole pipeline ran: tdd, qa, review,
mutants, and `keeler keeler-branch` green.
