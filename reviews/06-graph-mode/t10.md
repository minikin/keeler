Spec: 06-graph-mode
Task: t10
Commit: aa528145d91f11fa42ace6e38695262fca406c00
Verdict: pass

Three findings blocked. The feature-level gate was in the code and
nowhere else — no scenario, no test — so both skipping it and cleaning up
on a red branch survived; kept, specified, and pinned from both sides.
an_unlanded_task_keeps_its_worktree stood on main where the loop is
skipped whole, and was the only guard on the line that keeps a spawned
worktree from deletion under a running agent; moved, and it kills the
mutant that survived before. The recipe's own listing line described one
level on main; rewritten.

Not blocking, fixed anyway: the feature level cleaned up after every
spec's tasks, not its own; the fixture put feat/ at main's tip so
"unmerged into which" was one question; each level now names what it
leaves to the other; the scenario title and mutants.md say what is true.

Hand-planted mutants M7, M9, M10 each killed by at least one test that
let it live before this commit. The mutation gate itself measured
nothing — the deliverable is shell — and said so.

Left, with a reason: the two-spec test builds 43-other's worktree by
hand rather than through keeler-spawn, because spawn refuses a second
spec's task on this feature branch — which is the binding this test
proves land honours too.
