Spec: 06-graph-mode
Task: t12
Commit: 1837eeda4bc5ad8959458733590a554215ce08f3
Verdict: pass

Three findings blocked, and two of them meant the board did not implement
the scenario it is named after.

The `done` branch answered before the verdict branch, so `passed` was
dead code and the one clause a tick satisfies was the clause that skipped
the other two — a ticked task with no record read as closed, which is the
exact hole the rule was written for. And the record was read from the
feature branch's tree, where it does not live until someone merges: every
task said `incomplete` before a merge, finished or not, and `done`
after, record or not. The guard was absent at the one moment it is for.
The board asks the task's own branch while it exists, and this tree once
it is gone, and landing is decided by this tree rather than by a leftover
branch.

The third was a mutation-gate regression I introduced: the new record
check made the spec-to-branch binding guard survive removal, because
43-other's worktree was spared for the wrong reason. Its fixture carries
a record now, and the mutant dies again.

Two more rode along, both promised by the task line and neither
implemented: the landing never read the gate's verdict, so a task ticked
over a red gate had its worktree destroyed; and the record check sat
above the registered-worktree filter, so every ticked recordless task was
told its worktree had been left in place — including in this repository,
where T1 and T2 of this spec have no worktrees at all.

Four hand-planted mutants, four deaths, each to the test named after what
it protects. Restoring the graph-answers-first ordering re-broke and
re-fixed the stale-verdict case the morning had settled, which the suite
caught.
