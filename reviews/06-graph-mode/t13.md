Spec: 06-graph-mode
Task: t13
Commit: f2996735e96ab154417383185648110d4832c808
Verdict: pass

Found by running the tool, not by reading it. T1 of spec 07 carries a
verdict of 101 written by a runner that no longer exists — the one that
gated unconditionally after an agent died, measuring a worktree nobody
had touched. Every rule since reads that file correctly and reaches the
wrong conclusion: the board says failed, keeler-resume says the verdict
stands, and the task is locked with no way out the tool ever mentions.

Nothing here takes a verdict back. That is a judgement about whether a
run can be believed, and it stays the human's — the resume still refuses
while the file is there. What changed is that the board stops keeping the
way out a secret: beside a failed task it names the file and says
removing it is what makes the task resumable.

One mutant: silence the line, and the test named after the situation
fails. The mutation gate itself measured nothing — the deliverable is
shell — and said so.

Scope, honestly: this is the fourth defect that only a live run could
show, after the false failed, the silent log and the death that exits
zero. The pattern is not that the tests are weak but that they cannot
know what the world does; every one of the four came from watching a real
agent and none from reading the suite.
