Spec: 06-graph-mode
Task: t14
Commit: 0fa2eddf2e0a1903228d03b6a3fe336108261d45
Verdict: pass

The sixth defect a live run found, and the shortest-lived: T1 of spec 07
was resumed, and the resume executed the runner written the night before
— by a recipe with no --verbose, no stream-json, and no check for a
finished turn, all three of which had been fixed hours earlier. The task
went back to work carrying every defect we had spent the day removing.

The spec said "re-runs the task's runner", and the implementation was
faithful to it. The words were wrong: a runner is generated code, not a
record of the run, and it ages with the recipe that wrote it. A resume
re-runs the *task* — the worktree, the branch and the log are what it
keeps.

One generator now, _write-runner, called by the spawn that cuts a
worktree and by the resume that returns to one. Extracting it moved the
recipe above keeler-spawn's comment block, which severed that recipe from
its own documentation and dropped it out of `just --list` — caught by the
installer test that reads the listing, not by anything in spawn's own
suite.

One mutant: stop regenerating, and the test named after the distinction
fails.

Worth recording: writing this record, an unquoted heredoc ate a backticked
word and ran it as a command — the same defect this project fixed in the
runner this morning, reproduced by hand within the day.
