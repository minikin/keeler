Spec: 06-graph-mode
Task: t15
Commit: 256f10fa3c5e7da231ed3034d2e29564e86cb0e3
Verdict: pass

Four findings blocked, and two of them reproduced — on this very commit —
the false `died` the task exists to abolish.

A spec outside `specs/` had its graph read from a path built as
`specs/<slug>.md`, so the tick was never found and finished work was
called dead. And `keeler-resume` accepts a lowercase task id while the
graph prints uppercase, so a runner regenerated from it matched nothing:
the same task, answered differently depending on what a human typed.

The third was worse in kind. The tick was decided by the task branch's
own copy of the parser, because the runner runs in the worktree and the
path was relative — a branch that edits that script, which this project's
own tasks do, could certify its own box. The parser is the main
checkout's now, baked in when the runner is written.

The fourth was that two of the four clauses were pinned by no test: every
fixture that wrote the record also wrote the commits, and every one that
ticked did both, so the shapes overlapped and none of them isolated a
clause. Fixing it showed the commits clause could not fail at all once a
record and a clean tree were required — it is gone. A check nothing can
fail is not a check.

The block asks the branch now rather than the worktree, which makes the
sentence it prints true and closes the case where an agent walks off to
another branch and leaves the tree answering for a task it has left.

Every remaining clause dies to its own mutant. The record one needed a
fixture that fails on the record alone; the first attempt skipped the
tick as well and so pinned two clauses and therefore neither.

Left with a reason: a spec filename containing a command substitution
executes at runner-generation time. Reproduced against the pre-T15 tree,
so it is its own fix rather than this one's.
