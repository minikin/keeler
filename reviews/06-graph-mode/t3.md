Spec: 06-graph-mode
Task: t3
Commit: 1ad1a506f5e1fd067e6529eaa331b29724735f3a
Verdict: pass

- **A respawn destroyed the run it refused.** The already-spawned guard
  looked only for the worktree, so a branch whose worktree had been
  removed slipped past it: `rm -f "$exit_file"` deleted the previous run's
  verdict and `git worktree add` then failed with a raw `fatal:`. The
  board afterwards read `not spawned` for a branch holding all the work —
  the exact confusion the three-state design exists to prevent, reached
  through a refusal path. Fixed test-first in 980b465: the guard names the
  branch too, and nothing is created or deleted until the worktree exists.
- **A spec outside the repository spawned anyway.** `git status
  --porcelain -- <path>` fatals on a path outside the repository and
  prints nothing on stdout, so the differs-from-HEAD check read the fatal
  as a clean tree. Found in the conformance pass, fixed test-first in
  151da0e.
- **Three mutants survived the first suite** and were killed by better
  tests, not code changes (980b465): the unmet-dependency filter (the
  fixture's only blocked task had one need, so listing every need looked
  identical to listing the unmet ones — a task blocked on one need of two
  now tells them apart); "refuses before creating anything" did not cover
  `.keeler/`, so a `mkdir` hoisted above every refusal survived; and the
  stale-verdict `rm -f` was unasserted. A fourth survived the mutation
  stage itself: with the branch guard removed the recipe still exited
  non-zero and still printed the branch name, because git's own fatal does
  both — the test now asserts the refusal is the recipe's and that no
  `fatal:` reached the human (1ad1a50).
- **Two shipped clauses were unasserted** and now are: the prompt's "never
  open a pull request" half of _nothing is pushed_, and the `| tee` that
  makes `tmux attach` the live view the notes promise rather than an empty
  pane. The installer's `.keeler/` ignore entry shipped untested; an
  install into a generated project now asserts it.
- **`keeler-status` threw away the graph's own answer.** It read the
  report and discarded field 2, so a task ticked on main printed
  `not spawned`. It now says `done`, using the parser's word rather than
  inventing a second one.
- Mutation: the deliverable is shell, so `just mutants-diff` measured
  nothing ("No src/ changes — outside the mutation gate's reach"). Eight
  mutants were hand-planted in the two recipes instead — exact session
  matching, the verdict's provenance, the unmet filter, the run directory's
  position, the stale verdict, tee-versus-redirect, the branch guard, and
  the lowercasing — and all eight die.
- Scope beyond the eight scenarios, all deliberate: the
  spec-outside-the-repository refusal, the undefined-task refusal, the
  `not spawned` and `done` states of the board, and the stale-verdict
  `rm -f`. Each is tested; none contradicts a scenario clause.
- **Left open, for the human and for T5.** (1) `keeler-spawn` reads
  readiness from — and cuts the worktree off — `HEAD`, not main, while the
  Implementation Notes say "readiness is read from the spec **on main**".
  Spawning from an unlanded branch therefore founds the new branch on it.
  T3 did not fix this: the spec assigns the shared main-resolution helper
  to T5 ("the shared main-resolution helper it and `mutants-diff` both
  call"), and building a second one here is the disagreement that
  paragraph forbids. (2) `--allowedTools 'Bash(git:*)'` permits
  `git push`, which the _nothing is pushed_ clause forbids in prose only.
  The notes name cargo, just and git and say permissions are the spec's
  decision, not T3's, so this is a proposed amendment
  (`--disallowedTools 'Bash(git push:*)'`), not a silent change.
- Spec conformance: all eight scenarios map to same-named tests in
  `tests/spawn.rs`, plus `the_installer_checks_tmux_the_way_it_checks_just`
  and `the_installer_ignores_the_run_directory` for the deliverable's
  installer half, and one property — `one_lowercased_id_in_four_places` —
  pinning the branch, session, worktree and run paths to one slug and one
  lowercased id.
