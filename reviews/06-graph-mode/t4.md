Spec: 06-graph-mode
Task: t4
Commit: 1464cd0c20f57da68b9d81fb08fa26ff02c7ae36
Verdict: pass

The conformance pass mapped all three scenarios to tests named after
them, and found no behaviour no scenario asks for.

The scoped code review found eleven things. Four mattered and are fixed
in `1464cd0`: the shipped cov check announced success when it had
extracted nothing from either ref, so an adopter whose recipe is spelled
otherwise could lower the coverage bar unseen; `keeler-branch`'s guard
tested the working tree while its message claimed committed, letting a
local baseline measure a branch against itself; `job_block` swallowed
the next job's documentation; and one assertion was satisfied by every
refusal the check can emit.

Left, with reasons: the `cov` recipe's guard covers the literal block
but not the `_has_rust_targets` variable it interpolates — real, and
wider than this task's scenarios, so it belongs to a spec amendment
rather than a silent fix here. The fixtures never build the detached
merge-commit checkout CI actually produces; the behaviour was verified by
hand and is correct, so this is a coverage gap for a later task. And
`resolve()` is duplicated between two jobs because YAML has no way to
share shell short of a composite action.

The mutation gate measured nothing — the deliverable is shell and YAML —
and the hand-planted mutants died.
