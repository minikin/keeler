Spec: 06-graph-mode
Task: t4
Commit: c0764aa15f810af6ed75c9f4bd1dc931b88fb790
Verdict: pass

- **Two fixtures did not match the Given they claimed.** _Branch gates are
  diff-based only_ says "a task branch with changes" and the fixture was a
  bare directory; _A branch ticks its task and nothing else_ says
  `just keeler-graph` on main and the test called
  `scripts/keeler-graph.sh` against two loose files, so "on the branch"
  and "on main" were the harness's fiction rather than git's. Both are now
  real repositories: the gate runs on `keeler/99-fixture/t4` with a commit
  of its own, and the tick fixture is one repository whose main holds the
  unticked spec and whose branch holds the ticked one, with the recipe run
  in each checkout. "Nothing else" is now `git diff --name-only main HEAD`
  plus the exact two changed lines. Found in the conformance pass, fixed
  in 6398083.
- **A hand-planted mutant found an assertion that could not fail.**
  `job.contains("fetch-depth: 0")` matched the job's own error text — "is
  the checkout fetch-depth: 0?" — and not the checkout option, so a job
  checked out shallow passed the assertion whose whole purpose is to
  forbid that. **T7's review-record test carries the identical assertion
  and the identical hole**; both now go through
  `common::checks_out_full_history`, which reads the option line itself
  (5acd7ab). This is the second vacuous `contains` in this repository's
  gate tests; the first was T7's, found the same way.
- **`[^[:space:]]` in the shipped awk was a gate that could stop failing.**
  Ubuntu's default awk is mawk, which only learned POSIX character classes
  in 1.3.4. A `cov_recipe` that extracts nothing on both sides compares
  equal, so the cov half of the check would have passed silently rather
  than erred. `[^ \t]` is understood everywhere and extracts the shipped
  recipe byte-identically (c0764aa). The refusal's assertion was
  `contains("cov")`, which is satisfied without the word "recipe"; it is
  now `contains("cov recipe")`.
- **`just --list` listed the recipe by its last comment line**, which was
  the tail of the rationale: `"dev, then crap-delta, then mutants-diff"
  would stop being observable.` The rationale now sits above a real
  summary. The first version of that test passed on the broken listing —
  the aside happened to contain all three gate names — so it also requires
  the summary to read as a sentence rather than the tail of one (aa912e9).
  `keeler-graph`, `keeler-spawn` and `keeler-status` have the same defect
  in their listings; they are T2's and T3's lines and were left alone.
- Mutation: the deliverable is a Justfile recipe, a workflow job and a
  sentence, so `just mutants-diff` measured nothing ("No src/ changes —
  outside the mutation gate's reach"). Eleven mutants were hand-planted
  instead — gate order, `set -e`, the inverted baseline guard, the silent
  skip, each half of the CI check deleted, the base tip in place of the
  merge base, the whole Justfile in place of the cov recipe, a shallow
  checkout, the dropped keeler/* condition, and the branch rule stripped
  of `keeler-land` — and all eleven die.
- Scope beyond the three scenarios, all deliberate and all tested: the
  no-baseline guard (`crap-delta` fails outright without a committed
  baseline, so the recipe would be unrunnable in a project that has not
  turned the ratchet on — the guard mirrors `qa.md` and the shipped
  workflow verbatim), the short-circuit on the first failing gate, and the
  `just --list` summary. `tests/installer.rs`'s `USER_FACING_JOBS` gained
  `branch-baseline`, which is how the new job becomes a thing the
  installer test suite asserts an adopter receives.
- **Accepted limitation.** The CI check compares the `cov` recipe, as the
  spec words it. `_has_rust_targets` and `_no_src_msg` — the variables
  `cov`, `crap`, `crap-baseline` and `crap-delta` all interpolate — sit
  outside the recipe, so a branch could neuter all four gates without
  touching a byte the check reads. That is a wider concern than "a branch
  moved the baseline" and a wider check than this scenario asks for, so it
  is recorded rather than silently widened.
- **The generic review pass is thin, and that is a finding.** The built-in
  `code-review` skill was invoked and returned a review of a different
  tree — `feat/graph-mode` in another checkout, 22 files, without T4 — so
  its nine findings are about `scripts/keeler-graph.sh`, `keeler-spawn`
  and `install.sh`, and none of them about this task. They have been
  relayed upward for T1's and T8's owners; one of them ("`just
  keeler-branch` does not exist", which made every spawn end in a false
  `failed`) is what this task closes. A second, correctly scoped reviewer
  had not returned by the time this record was written. What stands in for
  it is the adversarial pass recorded above, which is where the mawk and
  vacuous-assertion findings came from.
- Spec conformance: all three scenarios map to same-named tests in
  `tests/branch.rs` — `branch_gates_are_diff_based_only`,
  `a_branch_that_moved_the_baseline_is_refused_by_ci`,
  `a_branch_ticks_its_task_and_nothing_else` — plus one property,
  `ci_passes_a_branch_exactly_when_it_left_the_whole_repo_state_alone`,
  pinning that CI passes a branch exactly when its diff leaves both pieces
  of whole-repo state alone. `just dev` is byte-identical and asserted so.
