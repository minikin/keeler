Spec: 06-graph-mode
Task: t5
Commit: 932b4a30c08fcc8602c932c060cd74d789df0a0b
Verdict: pass

- **Spec conformance.** All three scenarios map to same-named tests in
  `tests/land.rs`: `baseline_updates_happen_at_fan_in_on_main`,
  `a_branch_that_was_green_alone_can_still_redden_main`,
  `keeler_land_refuses_to_run_anywhere_but_main`. The shared helper — the
  other half of the deliverable — is pinned by `main_is_resolved_in_one_place`
  (the ref sequence occurs once in the `Justfile`; both `keeler-land` and
  `mutants-diff` call `_main-ref`) and `main_is_whatever_this_repository_calls_it`
  (a `master` repository, and a repository where `origin/main` outranks the
  local branch). One property, `any_branch_but_main_is_refused_by_name`,
  pins the refusal to the branch rather than to the one name a test picked.
- **Two branches of the refusal were untested**, found in the conformance
  pass and fixed test-first in 932b4a3: a detached HEAD — where "am I on
  main?" has no answer — and a `just crap-baseline` that fails outright,
  where nothing must be staged because nothing was regenerated. Neither
  had a test that would notice if the recipe stopped doing it.
- **Scope beyond the three scenarios, all deliberate and all tested.**
  (1) `keeler-land` says so and stages nothing when `crap-baseline`
  produced no file at all, which is what the shipped recipe does in a
  project with no Rust targets yet (`a_project_with_nothing_to_measure_stages_nothing`);
  without it `git add` would fail on a missing path and the run would end
  in a raw git error. (2) `keeler-spawn` now reads readiness from the spec
  **on main** through `_main-ref` — the first item T3's review left open
  for this task, since a second main-resolution there is the disagreement
  the notes forbid. Test: `readiness_is_read_from_the_spec_on_main` in
  `tests/spawn.rs`, red before the change (a branch's own tick unblocked
  T3), green after. It is a clause of the Implementation Notes rather than
  a scenario of its own.
- **How the red main is simulated.** The scenario's Given is two branches
  that were each green alone; the fixture renders it as a main whose
  `just dev` fails, because the observable behaviour under test is a red
  main and building two genuinely-conflicting Rust branches would put
  minutes of real gates inside a unit test. `just dev` and
  `just crap-baseline` are a PATH stub that records what it was asked to
  run, which is also what lets "gates first, baseline second" be asserted
  as an *order* rather than as two independent facts.
- **Mutation: the deliverable is shell**, so `just mutants-diff` measured
  nothing ("No src/ changes — outside the mutation gate's reach"). Eight
  mutants were hand-planted instead and all eight die: the branch guard
  removed; the baseline regenerated before the gate; a red main ignored
  rather than refused; the regenerated baseline left unstaged; the recipe
  committing the baseline itself; `_main-ref` preferring the local
  branches over the remote ones; `mutants-diff` carrying its own copy of
  the ref list; and `keeler-spawn` reading readiness from the working tree
  again.
- **Left open, for the human.** (1) `just dev` runs `fmt`, which rewrites
  files. On a main that was already dirty or unformatted, the tree after a
  land holds the staged baseline *and* those rewrites, so "exactly that
  staged change and nothing else" holds for the clean main a fan-in
  actually leaves, not for a dirty one. A refusal on a dirty tree would
  fix it and is behaviour no scenario asks for — a proposed amendment, not
  a silent addition. (2) A land whose baseline came out byte-identical
  still prints "review the diff and commit"; the diff is simply empty.
- **The generic `code-review` pass found nothing in this diff.** Its nine
  findings are all in code this task did not write — T3's `keeler-spawn`
  and `keeler-status`, T7's workflow, T8's installer — and are reported
  upward rather than fixed here. The two that touch T5's neighbourhood:
  `just keeler-branch` does not exist until T4 lands, so a real spawn
  records a failed verdict today; and `scripts/keeler-graph.sh` is still
  not in the installer's file set, which is T8's task.
