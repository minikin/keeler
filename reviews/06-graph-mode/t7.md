Spec: 06-graph-mode
Task: t7
Commit: ad08ce84b30b6bf86237578b03f7a09ba3e6a1b3
Verdict: pass

- The review-record check resolved the branch head only by name
  (origin/<ref> or local ref); a pull request from a fork carries the
  branch only as the detached HEAD, so every honest fork record would
  have failed. Fixed test-first in ad08ce8: the
  check falls back to HEAD, and the not-on-base half absorbs the extra
  ancestry that fallback admits.
- First probe of hand-mutated gate shell left two mutants alive
  (existence check and Commit:-count check individually droppable);
  the tests were strengthened to name "missing" and "found N" and all
  seven hand mutants now die.
- Spec-conformance: both scenarios map to same-named tests in
  tests/review_record.rs; fetch-depth: 0, the review-stage write
  instruction, and malformed-input behaviour are each pinned by an
  assertion, per the parked branch's three blockers.
