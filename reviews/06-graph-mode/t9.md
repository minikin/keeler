Spec: 06-graph-mode
Task: t9
Commit: a71fb5cce6c0aa1748bd65bc34b48f4aa189be6f
Verdict: pass

Two findings blocked, both the amendment stopping at the code rather than
reaching the documents that ship: four installed files still taught that
readiness is read from main, and the test named after the scenario T9
amended had never been moved onto a feature branch, so it would have
passed identically before and after. Both fixed, each with a test that
fails without the fix.

Four smaller: the graph was read through HEAD rather than the branch,
correct only while the guard above it held; `git branch --show-current`
needs git 2.22 and was unguarded; a spec name that cannot be a git ref
produced advice nobody could follow; and keeler-status's fallback to HEAD
was silent about which ref it had read.

Left for T10, and adopter-visible today: keeler-land runs only on main
and expects the ticks there, so between T9 and T10 a landed task's
worktree is not cleaned up and Status: is never staged. Also noted, not
this task's: keeler-status run from inside a task worktree resolves the
repository root to the worktree, so a spawned task reads "not spawned".

The mutation gate measured nothing — the deliverable is shell and
markdown — which the run says rather than reporting a pass over nothing.
