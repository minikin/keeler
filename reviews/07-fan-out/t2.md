Spec: 07-fan-out
Task: t2
Commit: edbb13b64dbda5f73ce5b49d989a955050283df9
Verdict: pass

The agent that wrote this reached its session limit after tdd, so review
and mutants are this record's, run afterwards.

Two blocked. The Format gate was red at HEAD — my merge of the two
parallel worktrees left two blank lines, and I committed without running
it. And the test for a green-gated task asserted `contains("passed")`
against a line that carries the fixture's own directory path: the fixture
was called board-passed, the board said died, and the substring matched
the path. The stub behind it predated the result-record check, so every
stubbed run died before its gate. Renaming the fixture failed the test.
It asserts the state field now, over a fixture that closes the task on
its branch so passed is reachable at all.

Thirty-three mutants were planted across keeler-fan-out and
_spawn-preflight. Five survived, all in the preflight and all named:
three are guards nothing tests anywhere (spec-not-a-file, ref-format,
committed-on-feature), two are genuinely equivalent because earlier
guards already guarantee what they check — which also means the
mktemp/trap/git-show machinery in the preflight is dead weight a later
task can simplify away.

The refactor holds: every ground keeler-spawn refused before, it refuses
now, in its own words, whichever recipe called it. The yes is exactly
yes/y case-aside and nothing else, refuses when there is nobody to ask,
and no command file sets the variable. After the yes the recipe stops —
no branch, no worktree, no session — which is T3's to change.

Three lesser fixes rode along: two substring assertions loose enough to
match `nothing else` and `already`; a keeler-spawn doc comment stranded
above _spawn-preflight, describing a recipe it no longer sits on; and
KEELER_FAN_OUT_YES, which children inherit — a spawned agent would have
received the human's answer already given, so it is unset before T3
spawns anything.

Left for later, recorded not hidden: the wave module and tests/spawn.rs
share twenty-seven identifiers and the whole fixture near-verbatim, which
is how the stub went stale. tests/common/mod.rs exists for exactly that
and is where they belong.
