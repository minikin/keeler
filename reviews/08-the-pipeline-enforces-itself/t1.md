Spec: 08-the-pipeline-enforces-itself
Task: t1
Commit: f0b1548c1874a41b19858176a749260da8dd3a1a
Verdict: pass

## Spec conformance

- Both of T1's scenarios have a test named after them in
  `xtask/src/pipeline/decision.rs`: `a_task_ticked_without_a_review_is_caught`
  and `a_reviewed_task_passes_and_the_gate_says_what_it_counted` — unit
  tests, as the task line specifies (T1 lists no acceptance tests; those
  arrive with the command in T5).
- The gate-soundness invariant is pinned by the property
  `the_gate_passes_exactly_when_every_ticked_task_is_covered`, whose model
  restates the coverage rule in exists-form against the implementation's
  all-form, and checks the missing set and the count exactly.
- No scope creep: `Verdict::Fail` handling is T1's own coverage rule
  ("a pass record or an unrecorded backlog line"), not T2's record
  grammar. Record-dominance and debt-monotonicity properties are left to
  T2 and T3 as the task list assigns them.
- The output names spec and task via `TaskId`'s `Display`
  (`<spec-slug>/<task-id>`), the address every artifact uses.

## Findings (settled)

1. The proptest seed file `xtask/src/pipeline/decision.proptest-regressions`
   was untracked while the tests that generated it were already committed;
   the testing conventions require committing seeds. Fixed: committed in
   f0b1548.
2. `TaskId::new` stored the task id verbatim with case-sensitive equality,
   while the workflow lowercases the id into every path — leaving T2's,
   T3's and T4's parsers three chances to disagree on case and fail a
   reviewed task. Fixed test-first in f0b1548: the id is lowercased at
   construction, pinned by `a_task_id_is_one_address_whatever_the_case`.

The generic code-review pass refuted its other candidates: the property
model is deliberately equivalent-but-differently-phrased, the distinct
count is intended and tested, and the empty-input pass
(`AllAccounted { ticked: 0 }`) is correct and pinned by the committed
seed.
