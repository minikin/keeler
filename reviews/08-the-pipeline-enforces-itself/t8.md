Spec: 08-the-pipeline-enforces-itself
Task: t8
Commit: 88e684a71aa3be818556615ecf86dc1bc883049d
Verdict: pass

Spec conformance: the one scenario T8 claims, _This repository runs the
check it ships_, maps to `this_repository_runs_the_check_it_ships` in
`tests/pipeline.rs`, the file the spec designates. The diff is the
verbatim job copy, a provenance note, and the test — no scope creep, no
other task's ground touched, T8's box left for /keeler:mutants.

Findings settled in this review (fixed at the commit above):

- The byte-equality assert kept `job_block`'s trailing blank line, so a
  job added below `review-record:` in either file — T7's `pipeline-check:`
  is already scheduled — would have failed the test over an invisible
  diff while the message claimed drift. Equality now compares with
  trailing whitespace trimmed, with the rationale in a comment.
- The template's five-line rationale comment had been hand-copied above
  the job in ci.yml, outside the compared block — a copy that could only
  drift silently. Dropped; the provenance note points at the template.
- `tests/pipeline.rs`'s header says its tests hold for every adopting
  project; the new test reads our own ci.yml. The spec-08 section now
  says it holds nowhere but here.

Findings noted and left open (out of T8's scope — the job is copied
verbatim by the spec's own mandate, so its script's defects belong to
the template, not the copy):

- The job's script accepts a ref name (`Commit: HEAD`) where it means a
  SHA, and `grep -c ... || true` swallows grep's error exit as zero. Both
  predate this task and ship to adopters; fixing them is template work
  under its own spec change, after which the copy is re-made.
- The copy pins `actions/checkout@v6` while ci.yml's other jobs use v7 —
  the price of verbatim, stated in the spec and in the provenance note.
  No dependabot config exists to trip over it today.
- The guard assertion style (`contains` on the `if:` expression) matches
  the convention of `tests/review_record.rs`; a line-exact helper beside
  `checks_out_full_history` would serve all three call sites, but that
  refactor spans other specs' tests.
