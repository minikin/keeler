Spec: 08-the-pipeline-enforces-itself
Task: t9
Commit: d9044cf74d701c36936d1c6002ece6ec80686c76
Verdict: pass

## Spec conformance

- T9's one scenario, _The rules stop claiming nothing can notice_, has an
  acceptance test named after it in `tests/pipeline.rs`, and it holds both
  Thens the scenario names. "They say the gate runs in the repository that
  ships Keeler": the review-stage paragraph names `cargo xtask
  pipeline-check` and no longer carries "there is no mechanism that will
  catch this for you". "And that an adopting project's review stage
  remains unenforced": it says the stage stays *documented rather than
  enforced* there and names the `keeler/*` pull-request check as the part
  of it they do have.
- Every phrase the test pins was absent from the pre-T9 docs and the
  banned one was present — checked against `b011f82`, the commit this
  branch was cut from: `documented rather than enforced` and `cargo xtask
  pipeline-check` occur zero times in that revision's `.claude/keeler.md`,
  `cargo xtask pipeline-check` zero times in its `CONTRIBUTING.md`, and
  `Nothing enforces this` once. So all six assertions were red before the
  change, not only the two the first red run reached before its assert
  stopped the test.
- The claims the new prose makes were verified against the code rather
  than recited from the spec: `pipeline_check_command` exists
  (`xtask/src/lib.rs:236`) and all three named callers really call it —
  `Justfile:118` under `dev` behind the `templates/keeler.yml` marker,
  `.github/workflows/ci.yml`'s `pipeline-check:` job, and the release
  guard through `skipped_pipeline` (`xtask/src/lib.rs:152,171`).
  `install.sh:274` lands the template at `.github/workflows/keeler.yml`,
  whose `review-record` job is gated on
  `startsWith(github.head_ref, 'keeler/')`, so "the `keeler/*`
  pull-request check in `.github/workflows/keeler.yml`" is the adopter's
  file under the adopter's path. `reviews/BACKLOG.md` holds exactly 39
  lines and they are 18 + 7 + 6 + 6 from specs 01–04 plus
  `06-graph-mode/t1` and `t2`, which is the breakdown CONTRIBUTING now
  states.
- Scope. Four files: the rules paragraph, CONTRIBUTING's Review debt
  section, the acceptance test, and a `CHANGELOG.md` entry under
  `[Unreleased]`. The changelog line is not named in the spec's Module
  list; it is here because `.claude/keeler.md` is installed verbatim into
  every adopting project, so this is a change to what they read — the one
  class of change CONTRIBUTING says must be logged. The earlier tasks of
  this spec added no entry because the gate itself never leaves this
  repository.
- `KEELER.md` was left alone deliberately. Its "except review, which is
  the one stage a machine cannot do for you and the one it cannot notice
  you skipping" is about the workflow Keeler ships, where — on the linear
  road — it remains true, and the spec names two files, not three. It is
  worth a later look: graph mode's `keeler/*` check already notices a
  missing record, so the sentence was already approximate before T9.
- T9 is assigned no invariant of its own. Gate soundness, record
  dominance and debt monotonicity belong to T1, T2 and T3, and their
  property tests ran green in the gate below.
- `Status:` untouched; the box is left for /keeler:mutants.

## Findings settled in this review (fixed at the commit above)

1. **A spec-06 test described a check it no longer made.**
   `the_rules_warn_that_a_skipped_review_goes_unnoticed` kept only
   `contains("leaves no artifact")`, while its comment and failure message
   claimed it verified that the rules say the stage is enforced here and
   not in an adopter's project. That claim moved to T9's test; the comment
   left behind would tell a reader a gate exists where it does not, and
   would have failed naming a claim T9 deliberately deleted. Its comment
   now says what it checks: the warning paragraph cannot vanish, and the
   phrase is the anchor both tests find it by.

2. **The scenario test asserted on a file the scenario does not cover.**
   It checked `.claude/keeler.md` — shipped — and `CONTRIBUTING.md` —
   this repository's own — under one name taken from the scenario, so a
   CONTRIBUTING-only regression failed under a heading pointing at the
   workflow rules. CONTRIBUTING's half is now
   `contributing_says_what_enforces_the_review_stage`, next to it.

3. **The test pinned the paragraph's line wrapping.** `cargo xtask
   pipeline-check` falls across a wrap in the finished prose, so the first
   green attempt would have required rewrapping the paragraph to suit the
   assertion — editing prose to fit a test. Both tests now compare the
   text with its line breaks taken out (`unwrapped`), so a phrase is
   looked for in the sentence and not in the layout.

## Findings noted and left open

4. **A prose gate is a substring gate.** Nothing here can tell a
   paragraph that means what it says from one that contains the right
   words; a rules file could satisfy every assertion and still mislead.
   That is the same limit the spec's Context states for the whole
   feature — "it cannot guarantee the review was any good" — and the
   phrases chosen are the load-bearing ones (`documented rather than
   enforced`, `keeler/*`, the command name), so a rewrite that drops the
   boundary fails. No fix proposed.

5. **`.claude/keeler.md` could not be edited with the Edit tool in this
   session** — the harness holds `.claude/**` as sensitive and this run
   is headless, so the paragraph was replaced by writing a unified diff
   into the worktree, `git apply`-ing it, and removing the scratch file
   with `git clean -f`. The result was checked with `git diff`: one hunk,
   the intended one. Worth knowing for the next spawned task that has to
   touch a file under `.claude/`.

## Verification

Part 1 above is mine. Part 2 was the built-in `code-review` skill at
medium effort over commit `19b4076`; findings 1 and 2 are its, both
confirmed and fixed at `d9044cf`. It found no correctness defects and
independently checked the three callers of the gate, the installer's
destination for `templates/keeler.yml`, the backlog's line count and
breakdown, and that the test-helper refactor left no unused import.

Gate: `just keeler-branch`, run twice — at `19b4076`, and again at
`d9044cf` because the two fixes above changed test code. The second run
is the one that counts: `just dev` green (fmt, clippy, 378 tests passed
and 1 skipped, coverage, CRAP with no function above the threshold),
`just crap-delta` 0 regressed / 1 improved / 36 new / 24 unchanged, and
`just mutants-diff` 107 mutants in 2m — 88 caught, 19 unviable, **0
missed**. Both runs exited 0 with the same three verdicts.
